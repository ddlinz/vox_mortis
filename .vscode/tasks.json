{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "docker-build",
            "label": "docker-build:vox",
            "dockerBuild": {
                "context": ".",
                "dockerfile": "Dockerfile",
                "tag": "vox:latest"
            }
        },
    ]
}